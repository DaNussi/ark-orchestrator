apiVersion: v1
kind: Service
metadata:
  name: {{ include "ark-orchestrator.fullname" . }}
  labels:
    {{- include "ark-orchestrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ark-orchestrator.selectorLabels" . | nindent 4 }}
