apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ark-orchestrator.fullname" . }}-test-connection"
  labels:
    {{- include "ark-orchestrator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ark-orchestrator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
